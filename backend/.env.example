# Environment Variables Configuration

# MongoDB Connection String
# Get this from MongoDB Atlas or your local MongoDB instance
# Example: mongodb://localhost:27017/blogger
# Example (Atlas): mongodb+srv://username:password@cluster.mongodb.net/blogger
MONGO_URI=your_mongodb_connection_string_here

# JWT Secret Key
# Generate a random secret key for JWT token signing
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_here

# Google Gemini API Key (Optional - for AI features)
# Get this from Google AI Studio: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Server Port (Optional - defaults to 5000)
PORT=8000

# Node Environment (Optional - defaults to development)
NODE_ENV=development
